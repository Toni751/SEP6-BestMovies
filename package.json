{
  "name": "sep6-bestmovies",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "^1.1.2",
    "@serverless-stack/resources": "^1.1.2",
    "@serverless-stack/static-site-env": "^1.1.2",
    "aws-cdk-lib": "2.24.0",
    "mongodb": "^4.5.0"
  }
}
